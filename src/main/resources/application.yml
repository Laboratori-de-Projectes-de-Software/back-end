spring:
  profiles:
    active: devel

---
server:
  port: 8082

spring:
  config:
    activate:
      on-profile: devel

  datasource:
    url: jdbc:mysql://127.0.0.1:3307/jaumesitos
    username: app
    password: app
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: always
      schema-locations: classpath:db/taules.sql

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

security:
  jwt:
    secret: 4FCcF1FzHjFdk8KjF3aSd9LK8g72MNp4kWjwNsT0PblYHRyMbjU8nFbOz9JZrPgG
    expiration: 3600000
