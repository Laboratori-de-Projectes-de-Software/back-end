spring.datasource.url=jdbc:h2:mem:ligabotstest;MODE=MariaDB;DATABASE_TO_LOWER=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# We add the MySQL Dialect so that it understands and generates the query based on MySQL
spring.sql.init.mode=never
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.defer-datasource-initialization=true

spring.jpa.show-sql=true
logging.level.org.hibernate.orm.jdbc.bind = trace

# JWT Configuration
jwt.secret=miClaveSecretaSuperSeguraParaGenerarTokensJWT12345
jwt.expiration=86400000

# Lost Password Configuration
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
mail.smtp.username=grupogrupolostpwd@gmail.com
mail.smtp.password=tehrijptsgubtjli
mail.smtp.auth=true
mail.smtp.starttls.enable=true

# URL del frontend TODO: cambiar el hecho de que este escrita la URL de esta manera
app.frontend-url=http://localhost:8080
